# CARD-0040 — Устранить системные сбои полного набора проверок

## HEAD

- Status: Planned — отдельный техдолг, принятый владельцем.
- Branch: не назначена.
- Head commit: `667639b` — свежая база; сбои воспроизведены перед синхронизацией.
- What changed: зафиксированы три независимых класса сбоев, чтобы они не
  блокировали поставку автоподбора из Плана.
- Evidence: `just check` находит два нарушения `staticcheck`; после
  `npm ci --prefix web` команда `just ui-check` даёт 3 падения из 101 теста;
  прежний `go test ./...` не завершился за лимит, а повтор с явным шестиминутным
  лимитом прошёл за 207 секунд, что сохраняет риск нестабильности по времени.
- Next action: назначить отдельную реализацию и добиться нулевого кода всех
  трёх команд.

ГОТОВО-КОГДА: файл knowledge/cards/CARD-0040-systemic-verification-debt.md
ГОТОВО-КОГДА: команда just check
ГОТОВО-КОГДА: команда just ui-check
ГОТОВО-КОГДА: команда timeout 6m go test ./...

## LOG

### 2026-08-09 — Verify

| Нарушение | Команда | Наблюдение |
| --- | --- | --- |
| Два нарушения Go static analysis | `just check` | `cards_http.go:37` — U1000; `pilot_config.go:132` — SA4006. |
| Три падения диалога | `npm ci --prefix web && just ui-check` | 98 из 101 теста прошли; падают выбор модели/история, сообщение rate limit и повтор отправки после ошибки в `Dialog.test.tsx`. |
| Go-набор ранее не уложился в лимит | `timeout 6m go test ./...` | На прежнем Verify был таймаут; текущий повтор PASS, самый долгий `internal/worker` — 207,263 с. Требуется проверить стабильность времени. |

Карточка создана отдельно по прямому решению владельца. Исправления не входят в
область CARD-0039 и должны проверяться собственным циклом поставки.
