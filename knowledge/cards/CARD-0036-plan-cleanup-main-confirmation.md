# CARD-0036 — Подтверждение очистки Плана в main

## HEAD

- Status: Verified PASS — ожидает решения человека о слиянии.
- Branch: `factory/e519e65b-769-a73c74d2-c23`
- Head commit: `acdb739` (проверенная ревизия поставки до записи результатов
  в эту карточку; следующий коммит меняет только карточку проверки).
- What changed: подтверждено, что очистка завершённых карточек уже поставлена в
  `main`; повторная реализация не создавалась.
- Evidence: 14 Python-тестов, `go test ./...`, `go build ./...`, web-тесты,
  typecheck и build прошли; трёхточечный diff содержит только CARD-0036 и
  CARD-0037. Девять прежних web lint-ошибок воспроизводятся отдельно.
- Next action: человек принимает решение о слиянии; lint-долг исправляется
  отдельно по CARD-0037.

## LOG

### 2026-08-09 — Verify

| Критерий | Проверка | Наблюдение |
| --- | --- | --- |
| Завершённая карточка не возвращается в План | `python3 -m unittest pilot/test_pilot.py -v` | Прошли 14 из 14; все 5 сценариев `PlanCardCleanupTest` подтверждают закрытие только после принятого финального этапа и исключение карточки из автозапуска. |
| Реализация не дублируется | `git diff --name-only origin/main...HEAD` | До записи проверки изменены только CARD-0036 и CARD-0037; `pilot/pilot.py` и `pilot/test_pilot.py` уже совпадают с `main`. |
| Смежные серверные части работают | `go test ./...` и `go build ./...` | Все Go-пакеты и сборка завершились с нулевым кодом. |
| Web-интерфейс не регрессировал | `cd web && npm test -- --run`, `npm run typecheck`, `npm run build` | Все 40 web-тестов, typecheck и production-сборка прошли. |
| Известный lint-долг изолирован | `cd web && npm run lint` | Ровно 9 errors и 7 warnings в файлах, перечисленных в CARD-0037; они не входят в diff поставки. |

### 2026-08-09 — Implement

По решению владельца сначала проверен свежий `origin/main`. Коммит `0db5ca9`
уже содержит логику очистки, спецификацию и регрессионные тесты, поэтому
дублирующие изменения не вносились. `python3 -m unittest
pilot.test_pilot.PlanCardCleanupTest` прошёл 5 тестов; `go test ./...` и
`go build ./...` завершились успешно.

### 2026-08-09 — Verify

| Критерий | Проверка | Наблюдение |
| --- | --- | --- |
| Завершённая карточка не возвращается в План | `python3 -m unittest pilot.test_pilot.PlanCardCleanupTest` | 5 из 5 сценариев прошли; закрывается только связанная карточка с принятым финальным этапом. |
| Нет повторной реализации | `git diff --name-only origin/main...HEAD` | Только CARD-0036 и отдельная CARD-0037 с учётом старого lint-долга; рабочий код и тесты уже в `main`. |
| Смежные серверные части не сломаны | `python3 -m unittest pilot/test_pilot.py`, `go test ./...`, `go build ./...` | 12 Python-тестов и все Go-пакеты прошли. |
| Web-поставка собирается | `npx tsc -p tsconfig.app.json --noEmit`, `npm run build` | Обе команды завершились успешно. |
| Полный web-набор зелёный | `npm test -- --run`, `npm run lint` | Заблокировано: 2 падения тестов Overview/Settings и 9 lint-ошибок в неизменённых UI-файлах. |

### 2026-08-09 — Implement

Владелец разрешил поставку, не связывая её со старыми UI-поломками. После
перебазирования целевой запуск `Overview.test.ts` и `Settings.test.tsx` прошёл
14/14: ранее падавшие `Overview active work` и `Settings` уже исправлены.
Оставшиеся девять lint-ошибок точно перечислены в отдельной CARD-0037.

### 2026-08-09 — Implement

Владелец принял содержательное ревью с вердиктом APPROVE: пять целевых
сценариев прошли, а последующие сбои были ошибкой разбора ответа ревьюера.
Поле `Head commit` приведено к фактически проверенной ревизии `1bbaa46`.

### 2026-08-09 — Implement

Поставка пересобрана от свежего `origin/main` без переноса загрязнённых
коммитов старой ветки. В состав вошли ровно CARD-0036 и CARD-0037; утверждение
о составе diff исправлено и теперь прямо называет обе карточки.

### 2026-08-09 — Implement

По решению владельца `Head commit` синхронизирован с фактически проверенной
ревизией поставки `3417c36`. Последующий коммит фиксирует только это уточнение
в CARD-0036 и CARD-0037 и намеренно не образует плавающую самоссылку.
