# CARD-0090 — Живой парк исполнителей и архив регистраций

Implementation commit: будет добавлен на стадии Implement + Test — продуктовый код этой работы намеренно не создаётся на стадии Specification.

## HEAD

- Status: Specification ready.
- Branch: `factory/9fbd1ec8-e5d-41952139-77c`.
- What changed: определены семисуточное архивирование по `last_heartbeat`, автоматическое возвращение по новому сигналу и сохранение 30-секундной границы `online`.
- Evidence: сверены `web/src/Workers.tsx`, `internal/controlplane/store.go`, `internal/protocol/types.go` и текущий контракт `GET /api/v1/workers`.
- Next action: реализовать разбиение списка и компонентные регрессии из спецификации.

## LOG

### 2026-08-12 — Specification

Старые регистрации не удаляются: клиент делит уже полученный API-список на
основной парк и архив по семи суткам от `last_heartbeat`. Offline до семи суток
остаётся в основном парке, а обновлённый heartbeat возвращает запись туда при
следующем refresh. Номер и путь карточки проверены по свежему `origin/main` и
опубликованным именам веток; существующая карточка другой работы не изменялась.
