# CARD-0034 — Мозг конвейера переключается после лимита подписки

## HEAD

- Status: Specification ready for implementation.
- What is changing: общий выбор «мозга» будет сохранять исчерпание Claude или
  Codex и на следующем запросе сразу выбирать живую подписку из `brain_chain`.
- Smallest approved slice: исправить передачу конфигурации в запись лимита и
  добавить изолированную Python-регрессию; маршрутизацию задач и интерфейс не
  менять.
- Evidence before implementation: `pilot.brain()` уже переходит к Claude после
  rate-limit Codex, но вызывает `note_limit` с двумя аргументами вместо трёх.
  Поэтому блок не сохраняется и следующий запрос снова тратит попытку Codex.
- Next action: Implement меняет только `pilot/pilot.py` и
  `pilot/test_pilot.py`, затем запускает обещанные проверки.

## LOG

### 2026-08-08 — Specification

Проверены `pilot/pilot.py`, `intake/app.py`, экран `Access` и `Overview`.
`brain()` используется для решений конвейера и голосовой постановки; он читает
`limits.json`, а `stage_worker()` уже исключает заблокированного провайдера при
выборе исполнителя этапа. Ветка предыдущей остановленной спецификации больше
не существует на `origin`, поэтому её нельзя безопасно перенести; план составлен
по текущему `main` и фактическому коду.
