# CARD-0047 — Выпуск обновляет собственные команды управления

## HEAD

- Status: Verified PASS — ожидает слияния.
- Branch: `factory/stability-audit`.
- What changed: штатный выпуск атомарно обновляет `/usr/local/bin/fx` и
  `/usr/local/lib/fx-factory-release` из той же ревизии, что сервер и воркер.
- Safety: частичная замена пары откатывается; синтаксис обоих shell-файлов
  проверяется до изменения живых команд.
- Browser: автоматическая установка серверного Chromium временно исключена из
  выпуска. Она вернётся отдельной поставкой только вместе с сетевой изоляцией и
  живой проверкой через `fx`.
- Evidence: Go — PASS; web typecheck — PASS; Vitest 123/123; установщики
  мозга, браузера и control tools — PASS; выпуск и откат — PASS.
- One next action: слить проверенную поставку и выполнить штатный выпуск.

## LOG

### 2026-08-10 — Stability audit

Живая Factory была выпущена из актуального `main`, однако установленный драйвер
выпуска остался старой версией и пропустил новый шаг установки браузера. Причина:
выпуск обновлял сервер, воркер, Pilot, Intake и санитар, но не обновлял сам
драйвер и брокер `fx`.

Проверка одновременно показала, что доставленный launcher Chromium ещё не имеет
обязательной сетевой изоляции. Поэтому слепая ручная установка не выполнена:
опасный шаг снят с автоматического выпуска до отдельного защищённого решения.

### 2026-08-10 — Verify

На чистом серверном клоне выполнены `go test ./...`, проверка типов интерфейса и
`npm test` (123/123). Отдельно прошли `test-install-brain.sh`,
`test-install-server-browser.sh`, `test-install-factory-control.sh` и
`test-fx-factory-release.sh`. Намеренный отказ при замене второй команды
управления подтвердил возврат обеих прежних файлов; отказ control installer в
сценарии выпуска подтвердил общий откат серверного и рабочего бинарей.
