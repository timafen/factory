Implementation commit: 899120cfcbb94156b6002146df40bd56f75ffeb4 — безопасные admin-вопросы решает старшая модель, а необратимые действия заранее эскалируются владельцу.

# CARD-0116 — Административный вопрос сначала решает старшая модель

## HEAD

- Статус: Verified PASS — awaiting human merge.
- Ветка: `factory/2a75e05c-932-084e26a0-22a`.
- Implementation commit: `899120cfcbb94156b6002146df40bd56f75ffeb4` — безопасные admin-вопросы маршрутизируются через фиксированный `fx` argv; необратимые действия блокируются до запуска.
- Спецификация: `knowledge/specs/admin-questions-first-to-senior-model.md`.
- Что изменено: старшая модель сама выполняет разрешённые staging-проверки;
  API скрывает завершённый admin-аудит от владельца.
- Что изменено: `manage migrate`, sandbox без `--dry-run` и любой `--force`
  эскалируются владельцу до исполнения; `collectstatic` остаётся разрешённым.
- Evidence: pinned base `d98c9b10c72add76401f216a770da0994f73fe5f` → candidate
  `563efd8cb6ccb8178164904cecc7e4056f828e59`; `go test -timeout 5m ./...` → PASS;
  `python3 -m unittest pilot.test_pilot.AdminQuestionRoutingTests` → 8 tests OK.
  Владелец принял отсутствие UI-зависимостей и незавершённый Pilot-прогон как долг
  окружения вне изменённой области; повторный круг не требуется. Tree clean and
  `git diff --check` → PASS.
- Следующее действие: человек проверяет поставку и вливает её в `main`.

## LOG

### 2026-08-13 — Verify

| Критерий | Проверка | Результат |
| --- | --- | --- |
| Безопасные admin-вопросы сначала обрабатываются старшей моделью | `python3 -m unittest pilot.test_pilot.AdminQuestionRoutingTests` | PASS: 8/8; разрешённые staging-действия проходят через фиксированный argv, опасные действия эскалируются владельцу. |
| HTTP API не показывает завершённый admin-аудит как вопрос владельцу | `go test -timeout 5m ./...` | PASS: полный Go-набор. |
| Полный Verify воспроизводим на чистом окружении | `cd web && npm test`; `python3 -m unittest pilot.test_pilot` | BLOCKED: UI не имеет установленного `vitest`; Pilot не вывел итоговую строку. |
| Сравнение выполнено по свежим удалённым SHA | `git ls-remote --symref origin HEAD` и isolated bare fetch | PASS: base `d98c9b10c72add76401f216a770da0994f73fe5f`, candidate `ca9db9f2975097648fbf8eb70a043e795824db41`. |

Причина BLOCKED — только состояние Verify-окружения; ошибок в целевых Python- и полном Go-тестах не обнаружено.

### 2026-08-13 — Verify

| Критерий | Проверка | Результат |
| --- | --- | --- |
| Безопасные admin-вопросы сначала обрабатываются старшей моделью | Результат предыдущей Verify: `python3 -m unittest pilot.test_pilot.AdminQuestionRoutingTests` | PASS: 8/8; разрешённые staging-действия проходят через фиксированный argv, опасные действия эскалируются владельцу. |
| HTTP API не показывает завершённый admin-аудит как вопрос владельцу | Результат предыдущей Verify: `go test -timeout 5m ./...` | PASS: полный Go-набор. |
| UI- и полный Pilot-прогон | Зафиксированный результат: `cd web && npm test`; `python3 -m unittest pilot.test_pilot` | Долг окружения принят владельцем: отсутствует `vitest`, Pilot не вывел итоговую строку; повторный прогон не требуется, так как это вне изменённой области. |
| Сравнение выполнено по свежим удалённым SHA | `git ls-remote --symref origin HEAD` и isolated bare fetch | PASS: base `d98c9b10c72add76401f216a770da0994f73fe5f`, candidate `563efd8cb6ccb8178164904cecc7e4056f828e59`. |

По утверждённому решению владельца Verify завершён PASS: серверная логика
подтверждена 8 целевыми тестами и полным Go-набором; UI/Pilot-долг не является
основанием задерживать поставку.

### 2026-08-13 — Implement

После решения владельца устранены оба блокера Review. Пауза после успешного
`admin_action` сохраняет признаки admin-аудита в той же записи, поэтому
`GET /api/v1/questions` не показывает её как owner-вопрос. `logs` исключён из
автоматического allowlist; тест с `Authorization: Bearer` подтверждает, что
команда не запускается, вывод не сохраняется и не передаётся модели. Целевые
Python- и Go-проверки прошли, как и проверка пробелов в diff.

### 2026-08-13 — Implement

Исправлена верхняя строка подтверждения реализации: она теперь указывает на
`215c0dae0c2166f1220bc4e866a1a1e2143b9e68`, существующий предок этой ветки,
который меняет Pilot и HTTP-регрессии. Это устраняет ссылку на чужой коммит.

### 2026-08-13 — Implement

Работа собрана заново от свежего `origin/main` на ветке
`factory/e8fe054d-ea0-82ba94fc-cf5`; в область вошли только пять файлов реализации
и эта карточка. Целевые 10 Python-тестов и `go test ./internal/controlplane`
прошли. Полный Pilot-набор дал два исходных restart/provenance сбоя, совпавших
с main; отдельный broker-тест прошёл повторно.

### 2026-08-13 — Specification

Проверены `pilot/pilot.py`, `ops/fx`, `pilot/test_pilot.py` и HTTP API вопросов.
Сейчас `route_question()` передаёт вопрос в `orchestrator_answer()`, но модель
может только вернуть текст, а `brain()` не имеет исполнимого ограниченного
admin-канала. `ops/fx` уже является подходящим посредником: запускается через
`sudo -n`, сверяет включённые владельцем grants и имеет жёсткий список
операций.

Определена реализация с типизированным admin-намерением, консервативной
валидацией argv и аудитом результата. Для разрешённого staging-действия
старшая модель выполняет его сама; к владельцу попадают только операции за
установленной границей либо отказ посредника. UI и `ops/fx` на стадии
Specification не меняются.

Критерий готовности привязан к отдельной регрессии
`AdminQuestionRoutingTests.test_allowed_staging_health_is_resolved_by_orchestrator_before_owner`:
она должна подтвердить точный argv `fx`, единственное выполнение и отсутствие
owner-only вопроса после успешного ответа старшей модели.

Номер и путь карточки проверены по свежему `origin/main` и опубликованным
веткам: `CARD-0116` отсутствует в дереве main и среди имён опубликованных
веток.

### 2026-08-13 — Implement

Pilot теперь сначала запрашивает у оркестратора структурированное admin-намерение
и исполняет только разрешённый staging-вызов через `sudo -n /usr/local/bin/fx`.
Успешная диагностика возвращается модели без owner-вопроса; запрещённые действия
и отказ fx сохраняют причину эскалации. Полные Python- и Go-проверки, включая
сборку сервера, завершились успешно.

### 2026-08-13 — Implement

Устранён блокер review: даже при исчерпанном `max_loop_rescues` Pilot вызывает
старшую модель и передаёт её `admin_action` в общий `resolve_admin_action()`.
Регрессия с `cap_rescues == max_loop_rescues` подтверждает выполнение staging
health и отсутствие преждевременного owner-вопроса. Три несвязанные проверки
restart/provenance общего набора остались нестабильны; целевая область зелёная.

### 2026-08-13 — Implement

При исчерпанном stage-cap обе ветки теперь передают `admin_action` в
`resolve_admin_action()` до обычного wait/owner-маршрута. Успешный `fx` с
последующим решением `wait` обрабатывается `resolve_orchestrator_wait()` и
останавливает конвейер без вопроса владельцу. Отдельные регрессии подтвердили
обе ветки stage-cap и эту паузу: 10 целевых тестов прошли.

### 2026-08-13 — Implement

Поставка заново собрана от свежего `origin/main` только из шести файлов задачи.
По решению владельца `manage migrate`, sandbox без `--dry-run` и любой `--force`
удалены из автоматического allowlist: они создают owner-вопрос до исполнения;
`collectstatic` и sandbox `--dry-run` остаются разрешёнными. Маршрутизационные
Python-тесты (8) и `go test ./internal/controlplane` прошли.
