Implementation commit: 8021a8f668e758f58807c47eea9e29b16ba22f08 — разрешённые административные вопросы staging сначала выполняет оркестратор через фиксированный fx argv.

# CARD-0116 — Административный вопрос сначала решает старшая модель

## HEAD

- Статус: Implemented.
- Ветка: `factory/b046adc4-f5b-32cddb13-7b6`.
- Implementation commit: `8021a8f668e758f58807c47eea9e29b16ba22f08` — маршрутизация admin-вопросов через безопасный staging fx.
- Спецификация: `knowledge/specs/admin-questions-first-to-senior-model.md`.
- Что изменено: Pilot принимает типизированное `admin_action`, пропускает только
  консервативный staging allowlist и запускает единственный фиксированный argv.
- Что изменено: успешный результат возвращается модели для ответа; служебный
  admin-аудит не выдаётся API как вопрос владельцу.
- Evidence: `python3 -m unittest pilot.test_pilot` → 253 tests OK;
  `go test ./...` и `go build ./cmd/factory-server` → OK.
- Следующее действие: Verify проверяет поставку и границы allowlist.

## LOG

### 2026-08-13 — Specification

Проверены `pilot/pilot.py`, `ops/fx`, `pilot/test_pilot.py` и HTTP API вопросов.
Сейчас `route_question()` передаёт вопрос в `orchestrator_answer()`, но модель
может только вернуть текст, а `brain()` не имеет исполнимого ограниченного
admin-канала. `ops/fx` уже является подходящим посредником: запускается через
`sudo -n`, сверяет включённые владельцем grants и имеет жёсткий список
операций.

Определена реализация с типизированным admin-намерением, консервативной
валидацией argv и аудитом результата. Для разрешённого staging-действия
старшая модель выполняет его сама; к владельцу попадают только операции за
установленной границей либо отказ посредника. UI и `ops/fx` на стадии
Specification не меняются.

Критерий готовности привязан к отдельной регрессии
`AdminQuestionRoutingTests.test_allowed_staging_health_is_resolved_by_orchestrator_before_owner`:
она должна подтвердить точный argv `fx`, единственное выполнение и отсутствие
owner-only вопроса после успешного ответа старшей модели.

Номер и путь карточки проверены по свежему `origin/main` и опубликованным
веткам: `CARD-0116` отсутствует в дереве main и среди имён опубликованных
веток.

### 2026-08-13 — Implement

Pilot теперь сначала запрашивает у оркестратора структурированное admin-намерение
и исполняет только разрешённый staging-вызов через `sudo -n /usr/local/bin/fx`.
Успешная диагностика возвращается модели без owner-вопроса; запрещённые действия
и отказ fx сохраняют причину эскалации. Полные Python- и Go-проверки, включая
сборку сервера, завершились успешно.
