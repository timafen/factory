# CARD-0034 — Автоподбор верхней карточки Плана

## HEAD

- Implementation commit: 332fca09a6d0261543fee0b0d4acb524b7259f30 — одноразово разведены исторические дубли номеров карточек.
- Status: Verified PASS — awaiting human merge.
- Branch: `factory/af59fb23-758-18b9659a-85a`
- Head commit: `3b95962` — реализация после rebase на свежий `origin/main`.
- Evidence: `python3 -m unittest pilot.test_pilot` — 24/24 OK; `just test` —
  PASS; `just ui-check` — 101/101 PASS; трёхточечный diff — только 5 файлов
  CARD-0034. `just check` и браузерный набор отдельно выявляют прежние сбои
  вне поставки, описанные в журнале.
- Next action: человеку проверить и слить поставку.

## LOG

### 2026-08-12 — Implement

Для одноразовой миграции исторического каталога добавлена проверяемая утилита
и применён её детерминированный план переименований. Полный SHA кодового
коммита выше остаётся стабильной ссылкой на реализацию; целевые тесты и
проверка уникальности номеров прошли.

### 2026-08-09 — Verify

| Проверка | Команда / наблюдение | Результат |
| --- | --- | --- |
| Приоритет, три слота и ожидание владельца | `python3 -m unittest pilot.test_pilot` | 24/24 OK: запускается верхняя карточка; три уникальные активные или ожидающие ответа работы блокируют четвёртую. |
| Повтор, новое планирование и ручной запуск | тот же набор | OK: повтор POST сохраняет `request_key`, новое планирование получает новое поколение, кнопка создаёт задачу с UUID-ключом. |
| Смежные проверки | `just test`; `just ui-check`; `just test-tooling`; `just test-launcher` | PASS; UI: 101/101 тестов. |
| Общие ворота | `just check`; `just test-browser` | BLOCKED вне поставки: `staticcheck` сообщает старые `U1000` и `SA4006` в двух файлах `internal/controlplane`; Overview не находит ожидаемый заголовок, поэтому 17 браузерных тестов не стартуют. |
| Границы и чистота | `git diff --name-only origin/main...HEAD`; `git diff --check` | Только пять файлов CARD-0034; пробельных ошибок нет. |

### 2026-08-09 — Implement

После ответа владельца восстановлен импорт `uuid` в обработчике Плана. Новый
регрессионный тест проходит ручной `action="task"` через `_promote()`, проверяет
UUID-ключ, созданную задачу и перевод карточки в работу. После rebase прошли
24 Python-теста; трёхточечный diff содержит только пять файлов CARD-0034.

### 2026-08-09 — Implement

После ответа владельца карточка без проекта возвращается в «новое» с понятной
причиной, а очередь продолжает запуск следующей подходящей работы. Новое явное
планирование получает отдельное поколение ключа; повтор одного запуска сохраняет
ключ. Пройдены 23 Python-теста, проверка типов, web build, 101 web-тест, полный
Go-набор и сборка.

### 2026-08-09 — Implement

После Review автоподбор перенесён в конец цикла с повторным чтением задач:
продолжение третьей работы больше не позволяет запустить четвёртую. Ключ запроса
теперь стабилен для карточки, поэтому повтор после потерянного ответа POST
идемпотентен. Оба сценария входят в 20 прошедших Python-тестов; полный Go-набор
и сборка прошли. Web-сборка не стартовала без установленных `node_modules`.

### 2026-08-09 — Implement

Поставка перебазирована на `origin/main`. При слиянии сохранены обе стороны
изменений уведомлений и тестов провайдеров; тесты изолированы от локального
файла прежней квоты. Автоподбор подтверждён 18 Python-тестами, весь Go-код —
`go test -timeout 5m ./...` и `go build ./...`; `npm run build` завершился нулём.

### 2026-08-09 — Verify

| Проверка | Команда / наблюдение | Результат |
| --- | --- | --- |
| Верхняя planned-карточка, лимит слотов, ожидание владельца и откат | `python3 -m unittest pilot.test_pilot` | 17/17 OK после rebase: запускается одна верхняя карточка; три уникальные активные/ожидающие работы блокируют старт; при ошибке или пустом `task.id` состояние остаётся `planned`. |
| Интеграция с циклом и границы диффа | `git diff --name-only origin/main...HEAD`; вызов `autostart_plan` в основном цикле | В диффе только `pilot/pilot.py`, `pilot/test_pilot.py`, спецификация и CARD-0034; запуск обёрнут в обработку ошибки. |
| Полный Go-набор | `go test -timeout 5m ./...` | PASS: все пакеты, включая `internal/controlplane` и `internal/worker`. |
| Линтер, типы, сборка и запуск | `npm run lint`; `npm run typecheck`; `just test-tooling`; `just test-launcher` | PASS. |
| Полный `just check` | `just check` | BLOCKED: `gofmt` на `internal/controlplane/limits_http.go`, который так же не отформатирован в `origin/main`; файл вне диффа. |
| Статический анализ | `just staticcheck` | BLOCKED: существующие `U1000` в `internal/controlplane/cards_http.go` и `SA4006` в `internal/controlplane/pilot_config.go`; оба файла вне диффа. |
| Компонентные и браузерные тесты | `npm test`; `npm run test:browser` | Компонентные: PASS, 98/98 после rebase. Браузерные: BLOCKED, Overview не находит ожидаемый заголовок; после первого сбоя 17 тестов не стартовали. UI-файлы вне поставки. |

### 2026-08-09 — Implement

Ветка перебазирована на `origin/main` (`0db5ca9`). На чистом main
`npm test -- --run` не прошёл (4 из 98, включая Overview и Settings),
что подтвердило независимую регрессию. Целевые 16 Python-тестов,
`go test ./...`, `go build ./cmd/factory-server`, TypeScript и Vite прошли.

### 2026-08-09 — Verify

| Проверка | Команда / наблюдение | Результат |
| --- | --- | --- |
| Верхняя planned-карточка | `python3 -m unittest pilot.test_pilot` | Пройдены 11 тестов: верхняя карточка создаётся на Triage с нужным контекстом. |
| Свободный слот и вопрос владельца | тот же тестовый набор | Три уникальные активные или ожидающие владельца работы блокируют новый старт. |
| Деньги и непустой идентификатор | тот же тестовый набор | Ошибка `create_task` и пустой `task.id` не переводят карточку в `in_work`. |
| Сборки и типы | `go build ./cmd/factory-server`; `go test ./...`; `npx tsc -p tsconfig.app.json --noEmit`; `npx vite build` | Все четыре проверки прошли. |
| Полный веб-набор | `npm test -- --run` | BLOCKED: 2 из 37 тестов не прошли: `Overview` ждёт кнопку у активной работы; `Settings` ждёт старую структуру `notify_groups` без `escalate`. |

### 2026-08-08 — Implement

Убрана посторонняя правка `web/src/Overview.tsx`; трёхточечный diff от
`origin/main` содержит только четыре заявленных файла. Автоподбор подтверждён
командой `python3 -m unittest pilot.test_pilot` (4 tests, OK), весь Go-код —
`go test ./...` (PASS) и `go build ./...` (PASS).

### 2026-08-08 — Specification

Зафиксированы слот менее трёх уникальных работ, учёт ожидания владельца,
денежные пределы, Triage-контекст и тихий журнал `routine`.

### 2026-08-08 — Implement

С разрешения владельца реализация выполнена отдельной поставкой на свежем
`origin/main`. Автоподбор, приоритет Плана, три уникальных слота, ожидание
владельца и сохранение `planned` при отказе доказаны четырьмя Python-тестами;
полные `go test ./...` и `go build ./...` также прошли.

### 2026-08-08 — Implement

После ревью автоподбор требует непустой `task.id` перед переводом карточки в
`in_work`. Отсутствующий идентификатор вызывает ошибку и не меняет карточку;
`python3 -m unittest pilot.test_pilot` подтвердил это в составе 11 тестов.

### 2026-08-09 — Implement

Готовая поставка перебазирована на свежий `origin/main`; трёхточечный дифф
по-прежнему ограничен четырьмя заявленными файлами. Целевые 17 тестов, полный
Go-набор, сборка и 98 компонентных UI-тестов прошли. `just check` подтвердил,
что ворота блокируют только прежние `U1000` и `SA4006` вне этой поставки;
Playwright локально не запускался согласно решению владельца.

### 2026-08-12 — Implement

После rebase на свежий `origin/main` утилита обнаружила добавленную параллельно
коллизию `CARD-0108`, назначила карточке свободный номер `CARD-0147` и
исправила её точную ссылку. Повторный dry-run сообщает об отсутствии дублей;
целевые проверки утилиты и резервирования номеров Пилота прошли.
