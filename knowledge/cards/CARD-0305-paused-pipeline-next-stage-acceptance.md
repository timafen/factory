Implementation commit: bd9629268825db1c7e7457fd103b5f49490f6243 — Pilot перечитывает снятую durable-паузу до сторожа в текущем цикле

# CARD-0305: Приёмка продолжения после снятой паузы

## HEAD

Status: Specified
Branch: `factory/b6a71e65-8b2-2e8c62e2-9f9`
Specification: `knowledge/specs/paused-pipeline-next-stage-acceptance.md`
What changed: определён сквозной regression-сценарий, который после внешнего
снятия паузы в том же цикле создаёт ровно следующую настроенную стадию и
подавляет дубли.
Evidence: фактические `cleanup_orphaned_paused_pipelines`, `pipeline_watch` и
порядок их вызова в `cycle` сверены; синхронизация durable config уже находится
в указанном implementation commit на `main`.
One next action: добавить сценарий в `pilot/test_pilot.py` и выполнить два
целевых тестовых класса.

## LOG

### 2026-08-15 — Specification

Зафиксирован контракт приёмки: устаревшая пауза в памяти заменяется значением
из durable config до сторожа; после выдержанного `STALL_WAIT=450` сторож берёт
максимальную успешную стадию и создаёт ровно следующую с её repository и
настроенными revision/worker. Оставшаяся пауза, живая следующая задача и
повторный цикл не создают дублей.

Предложенный предыдущей стадией `CARD-0302` не использован, потому что номер
уже занят опубликованными карточками других работ. Для этой работы выбран
отдельный свободный `CARD-0305`; существующие карточки не менялись.
