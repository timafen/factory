# CARD-0043 — Ворота выката проверяют тесты до установки

## HEAD

- Status: Specification ready — ожидает реализации.
- Branch: `factory/7365008c-9b0-3648f385-848`.
- Specification: `knowledge/specs/factory-release-test-gates.md`.
- What changes: перед установкой бинарей release-скрипт обязан выполнить UI unit,
  Go и собственную release-regression проверку наряду с уже существующими типами
  и сборками; любой отказ не затрагивает бинарей и служб.
- Decision: владелец утвердил только локальные детерминированные проверки;
  внешние, browser и ручные проверки исключены из ворот.
- One next action: реализовать два файла, перечисленные в спецификации.

## LOG

### 2026-08-09 — Specification

Подтверждено: текущий `ops/fx-factory-release` после `npm ci` выполняет только
TypeScript-проверку, Vite-сборку и `go build`; перед установкой нет ни UI unit-,
ни Go-, ни release-regression-теста. Спецификация фиксирует три обязательных
ворота и сценарии их безопасного отказа. Отдельно учтено, что shell-регрессия
сама запускает release-скрипт: её фикстура должна перехватывать этот вложенный
вызов, чтобы проверить ворота без рекурсии.
