# CARD-0177 — Название поставки из подтверждённого результата

Implementation commit: b74f3050338e23c3d5ea8ba2f729254b919b3340 — повторное слияние сохраняет понятное название результата и устойчивый маркер работы.

## HEAD

- Status: Implemented
- Branch: factory/7f3c2e58-260-4e970483-d97
- Что изменилось: после перебазирования на свежий `main` Pilot одновременно передаёт в повторное слияние подтверждённое человеческое название результата и `work_id`; защита от дублирующих PR не теряется.
- Evidence: `python3 -m unittest -v pilot.test_pilot.DeliveryTitleTests pilot.test_pilot.MergeConflictRecoveryTests` → 18 tests, OK; `python3 -m unittest pilot.test_pilot` → 354 tests, OK (13 skipped); `python3 -m py_compile pilot/pilot.py pilot/test_pilot.py` → exit 0.
- Следующее действие: повторно провести Review перебазированной поставки.

## LOG

### 2026-08-15 — Implement

Добавлен строгий выбор названия поставки из подтверждённого коммита или явного override без fallback на исходную проблему. Название сохраняется при рестарте и merge conflict, попадает в PR, receipt, release wait и успешные/аварийные уведомления. Целевые 7 тестов и полный набор из 350 тестов прошли успешно; синтаксис обоих Python-файлов проверен.

### 2026-08-15 — Implement

Устранено наследование названия по одному лишь исходному тексту: повторное использование теперь требует одинакового непустого `work_id`, а intent одной поставки восстанавливается из собственного сохранённого состояния. Регрессионный тест подтверждает независимый выбор двух результатов при одинаковом исходном названии; 8 целевых и все 351 тест Pilot прошли.

### 2026-08-15 — Implement

Ветка перебазирована на свежий `main`; конфликт разрешён с сохранением и человекочитаемого названия результата, и устойчивого `work_id` в PR. Регрессионная проверка подтверждает обе гарантии при восстановлении слияния; 18 целевых и все 354 теста Pilot прошли, синтаксис Python проверен.
