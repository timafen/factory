# CARD-0030 — Падающие тесты и линт, не связанные с type-fix интерфейса

## HEAD
- Status: Implemented in working branch — ожидает Verify PASS; в `main` ещё не влито
- Branch: factory/dd5330d5-8ef-8fb7f15b-de2
- Head commit реализации: 9288d93; type-fix — a38548b, базовая точка — 342f142
- Точка кода: `web/src/Say.tsx`, `web/src/TaskDetail.tsx`, `web/src/types.ts`
- Что изменить: почистить перечисленные ниже падения тестов и линта; они существовали
  до type-fix и не были им вызваны
- Evidence: `npx tsc -p tsconfig.app.json --noEmit` → PASS; `npm run build` → PASS.
  Старые падения Go/Vitest/lint не вызваны type-fix — см. сравнение ниже
- One next action: провести Verify; только после PASS влить изменения в `main` squash-ем

## Падения (идентичны на 342f142 и на a38548b)

**Go: `go test ./...`**
- `internal/controlplane`: `TestHTTPManagedRepositoryCatalog` — `http_test.go:152`,
  ожидался статус 200, получен 404 (page not found) при повторном `POST /api/v1/repositories`
  с уже существующим `remote_identity`

**Web: `npx vitest run` — 15 из 72 тестов красные, все в `src/App.test.tsx`**
- renders truthful retained metrics on the default overview
- marks only exact navigation destinations as the current page
- creates, revises, and disables a Workflow through its bounded views
- loads additional Automation and Occurrence pages
- pins an enabled Workflow revision while preserving free-text context
- renders every task status in the operational board
- loads another bounded task page without duplicating existing work
- polls only the live head page after older work is loaded
- does not retain tasks shifted out of the live head without loading history
- exposes a new history cursor when the live head grows beyond one page
- reopens exhausted history from a changed live-head boundary without duplicates
- restricts repositories to the selected worker and warns for offline work
- confirms permanent deletion only for terminal task history
- does not restore deleted work when an older history request finishes late
- delegates a configured managed repository to the selected worker

**Web: `npm run lint`**
На 342f142 (до type-fix) — 12 ошибок, 5 предупреждений:
- `Access.tsx:35:26`, `Live.tsx:38:10`, `Overview.tsx:60:10`, `Pipeline.tsx:31:34`,
  `Say.tsx:132:7` — `react-hooks/set-state-in-effect` (setState синхронно внутри эффекта)
- `Say.tsx:175/221/238/257/284:14` — `@typescript-eslint/no-unused-vars` (`'e' is defined but never used`)
- `Say.tsx:394:54` — `react-hooks/refs` (доступ к ref во время рендера)
- `TaskDetail.tsx:337:41` — `@typescript-eslint/no-unused-vars` (`'taskId'`)
- `Speak.tsx` — 5 предупреждений `react-refresh/only-export-components`

На a38548b (после type-fix) список тот же, кроме `TaskDetail.tsx:337:41` — эта ошибка
устранена самим type-fix (убран неиспользуемый параметр). Итог: 11 ошибок вместо 12,
регрессий нет.

## LOG

### 2026-08-08 — Implement
По решению владельца исправлена недостоверная текущая сводка: указаны настоящие
файлы type-fix и рабочая ветка доставки, удалено утверждение о состоявшемся мёрже
в `main`. Перед production-сборкой выполнен `npx tsc -p tsconfig.app.json --noEmit`
(PASS), затем `npm run build` (PASS). Следующий шаг — Verify; мёрж в `main` допустим
только squash-ем после Verify PASS.

### 2026-08-08 — Implement
Стадия Implement+Test (задача «Починить ошибки типов в интерфейсе Фабрики», 3/5):
по указанию владельца сравнили падения на 342f142 (код без type-fix) и на a38548b
(код с type-fix, тот же коммит, что проверяла стадия Verify). Списки падений
`go test ./...`, `npx vitest run`, `npm run lint` идентичны на обеих точках —
type-fix не добавил ни одной регрессии, а даже устранил один lint-error
(`TaskDetail.tsx` unused `taskId`). Решение владельца: не блокировать мёрж этими
старыми падениями, зафиксировать их здесь и мёржить squash-ем после того, как
новых регрессий не появится.
