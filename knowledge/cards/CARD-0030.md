# CARD-0030 — Ошибки типов в интерфейсе Фабрики

## HEAD
- Status: Implemented and live-verified PASS.
- Branch: `factory/ace8e1f5-b9d-b70216a3-975`
- Head commit: `4f76a7d`.
- Что изменилось: тесты приведены к текущей навигации и API `limit=200`; API
  настроек читает и сохраняет боевой массив `stages`, показывает `max_work_rounds`
  и восстанавливает `allowed_workers` из маршрутов и зарегистрированных workers.
- Evidence: Vitest 75/75; TypeScript и production build → PASS; `go test ./...`
  → PASS; локальные GET/PUT/restore → HTTP 200, `8 → 9 → 8`; `/settings` → 200.
- One next action: влить ветку в `factory-custom/main`.

## LOG

### 2026-08-08 — Implement

Исправлены 15 устаревших UI-проверок без ослабления пагинации, статусов и
маршрутизации. Живая проверка выявила и устранила несовместимость API с реальным
`config.json`: массив `stages`, параметр `max_work_rounds` и отсутствующий в старом
файле `allowed_workers`. Релиз `4f76a7d` выкачен через `fx factory release`.

Доказательства: `npm test -- --run` → 75/75; `npx tsc -p tsconfig.app.json
--noEmit`, `npm run build`, `go test ./...` → PASS. Локальный API до сохранения
вернул `max_work_rounds=8`, после PUT — `9`, после восстановления — `8`; все три
ответа HTTP 200, warnings пусты, `/settings` → HTTP 200.

### 2026-08-08 — Verify

| Критерий | Команда / проверка | Результат |
| --- | --- | --- |
| Поставка в `factory-custom` | PR #3, затем PR #4; `git merge-base --is-ancestor` | PASS: merge-коммиты `bf06a5f`, `45c822b` |
| Настройки: компиляция | `cd web && npx tsc -p tsconfig.app.json --noEmit` | PASS |
| Регрессии UI | `cd web && npm test -- --run` | FAIL: 15 из 75 тестов, все в `src/App.test.tsx` |
| Регрессии Go | `go build ./cmd/factory-server && go test ./...` | PASS |
| Релиз | `sudo -n /usr/local/bin/fx factory release` | PASS до проверки TypeScript; после исправления маркеров конфликта ошибок не вывел |
| Живой экран | `curl -L https://factory.timafen.com/settings` | BLOCKED: HTTP 401 Basic Auth, учётные данные не предоставлены |

В интеграции были удалены оставшиеся маркеры Git из `TaskDetail.tsx`; этот
исправляющий коммит слит в `factory-custom` через PR #4. Необходима отдельная
починка упавших UI-тестов и авторизованная проверка чтения/сохранения `/settings`.

### 2026-08-08 — Implement
По решению владельца type-fix `a38548b` перенесён на чистую ветку от свежего
`main`. Cherry-pick оказался пустым после разрешения конфликта: исправления
`TaskDetail.tsx` и `types.ts` уже находятся в `main`, а удалённый `Say.tsx`
намеренно не восстановлен. Чистая установка зависимостей, TypeScript-проверка и
production-сборка завершились успешно.

В старой ветке `factory/dd5330d5-8ef-8fb7f15b-de2` оставлена нерассмотренная
функциональность новых экранов и API (4 121 строка в 35 файлах). В эту ветку она
не переносилась; решение о ней следует оформить отдельной задачей владельца.

### 2026-08-08 — Verify

| Критерий | Команда / проверка | Результат |
| --- | --- | --- |
| TypeScript интерфейса | `npx tsc -p tsconfig.app.json --noEmit` | PASS |
| Production-сборка UI | `npx vite build` | PASS |
| Регрессии UI | `npm run lint`; `npm test` | PASS: 72/72 теста |
| Сборка сервера | `go build ./cmd/factory-server` | PASS |
| Живой сервис | `systemctl is-active`; HTTPS-запросы | active; `/` и `/healthz` → 401 без авторизации |
| Рестарт для доставки | `systemctl restart factory-server.service` | BLOCKED: требуется интерактивная авторизация |

`go test ./...` начал успешную проверку пакетов сервера, worker и control plane,
но полный прогон не удалось завершить до рестарта: после попыток параллельного
запуска оставшиеся пакеты ожидают завершения. В рабочем дереве нет отладочных
или посторонних файлов; `git diff --check` проходит.

### 2026-08-08 — Implement

Владелец уточнил модель доставки: до слияния сервис работает из `main`, поэтому
его рестарт не проверяет эту ветку и не входит в критерии приёмки. Результат принят
как PASS по уже зафиксированным зелёным проверкам TypeScript, Vite, ESLint,
72/72 Vitest-тестам и `go build`; повторные проверки по указанию не запускались.

### 2026-08-08 — Verify

| Критерий | Команда / проверка | Результат |
| --- | --- | --- |
| TypeScript интерфейса | `cd web && npx tsc -p tsconfig.app.json --noEmit` | PASS, ошибок нет |
| Production-сборка UI | `cd web && npm run build` | PASS, Vite собрал UI за 7.04 s |
| Регрессии UI | `cd web && npm run lint && npm test` | PASS, ESLint без замечаний; 72/72 Vitest-теста |
| Сервер и регрессии Go | `go build ./cmd/factory-server && go test ./...` | PASS, все пакеты прошли; самый долгий `internal/worker` — 187.585 s |
| Чистота поставки | `git diff --check origin/main...HEAD`; `git status --short` | PASS, только ожидаемое изменение карточки перед коммитом |

Проверки выполнены после `cd web && npm ci`; npm сообщил о двух high-severity
уязвимостях зависимостей — они существовали ранее и не относятся к этой задаче.
Изменения типов `TaskDetail.tsx` и `types.ts` уже присутствуют в `main`; diff
данной ветки добавляет только карточку с подтверждением результата.
