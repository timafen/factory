Implementation commit: e43462307fcd7c25003eecfe693fd21a9dfe8ba7 — поставлен единый экран вопросов владельца `/answer`.

## HEAD

- Статус: Implement завершён как дубликат уже поставленного экрана.
- Branch: factory/bb9ce3d9-e95-2a31a38e-e71
- Implementation commit: e43462307fcd7c25003eecfe693fd21a9dfe8ba7 — меню «Нужен ответ», список открытых вопросов и быстрые ответы.
- Что изменилось: продуктовый код не менялся; подтверждено, что `/answer` покрывает сценарий этой заявки.
- Evidence: `npm test -- --run src/Answer.test.tsx` → 1 тест прошёл; `npm run build` → успешно; `npm test` → полный набор прошёл.
- Следующее действие: отдельная работа только при появлении новой регрессии экрана `/answer`.

## LOG

### 2026-08-12 — Specification

Specification завершена: CLOSE / DUPLICATE, реализация не требуется.

Пункт «Нужен ответ» уже ведёт владельца к единому списку открытых вопросов,
показывает их количество и позволяет принять решение одним нажатием. Текущее
предложение не создаёт второй экран и не переименовывает существующий пункт.

Решение владельца:

- закрыть предложение как дубликат PR #196;
- сохранить название «Нужен ответ»;
- не создавать отдельное предложение о переименовании в «Решения»;
- не считать свёрнутую историю основанием для новой задачи.

Граница работы: только `knowledge/specs/solutions-menu-duplicate-pr-196.md`
и эта карточка; продуктовые файлы, UI, API, тесты и история другой карточки
не изменяются.

### 2026-08-12 — Implement

Проверен уже поставленный commit реализации из PR #196. В `/answer` есть меню,
счётчик открытых вопросов, однострочный список с ellipsis и действия «Делаем» /
«Не делаем» с POST на endpoint ответа. Новый экран и переименование не добавлялись.
Целевой тест прошёл (1/1), сборка и полный Vitest-набор прошли; продуктовый diff
отсутствует.
