# CARD-0031 — Экран «Работа»: повторный этап и нагрузка CPU

## HEAD

- Status: Implemented
- Branch: `factory/7045eb03-0db-78e3a5c3-a81`
- Head commit: `0911e19`
- What changed: метка «заново» ставится на повторно запущенной текущей стадии; уже пройденные стадии справа сохраняют исторический статус.
- What changed: обзор объясняет загрузку процессора числом активных работ и фактическими слотами, не выдумывая занятые места при отсутствии данных API.
- Evidence: целевой Vitest → 4 passed; `tsc -p tsconfig.app.json --noEmit` и Vite build → passed.
- Next action: открыть `/work` с повторно запущенной стадией и обзор `/` для визуальной приёмки.

## LOG

### 2026-08-08 — Implement

На свежий `origin/main` точечно перенесено поведение CARD-0031 без настроек Фабрики и CARD-0030. Регрессионные тесты проверяют повторный Implement после Review, первый запуск стадии и оба варианта доступности данных слотов. Целевой Vitest, обязательный TypeScript и production build прошли; полный Vitest в ограниченной среде не успел завершиться, а общий lint остаётся красным на существующих ошибках вне карточки.
