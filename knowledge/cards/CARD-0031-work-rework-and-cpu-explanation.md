# CARD-0031 — Экран «Работа»: повторный этап и нагрузка CPU

## HEAD

- Status: Implemented
- Branch: `factory/d3b59c1c-3eb-d73aa853-2d2`
- Head commit: `070537b` (предметный код и тесты; карточка фиксируется следующим коммитом доставки)
- What changed: метка «заново» ставится на повторно запущенной текущей стадии; пройденные стадии справа сохраняют исторический статус.
- What changed: обзор объясняет загрузку процессора числом активных работ и фактическими слотами, не выдумывая занятые места при отсутствии данных API.
- Evidence: целевой Vitest — 4 passed; `npx tsc -p tsconfig.app.json --noEmit` — passed; production build — passed.
- Next action: открыть `/work` с повторно запущенной стадией для визуальной приёмки.

## LOG

### 2026-08-08 — Implement

На свежий `origin/main` перенесены только изменения CARD-0031 без файлов других задач. Регрессионные тесты проверяют повторный Implement после Review, первый запуск стадии и оба варианта доступности данных слотов. Целевой Vitest, обязательный TypeScript и production build прошли. Полный Vitest: 64 passed, 15 существующих падений в `App.test.tsx`; общий lint: 11 существующих ошибок и 6 предупреждений вне логики карточки.
