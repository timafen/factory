# CARD-0031 — Экран «Работа»: повторный этап и нагрузка CPU

## HEAD

- Status: Implemented
- Branch: `factory/9a0f1e86-007-069efe8d-889`
- Head commit: `fa90fb4`
- What changed: метка «заново» ставится только на повторно запущенной текущей стадии; пройденные стадии справа сохраняют исторический статус.
- What changed: обзор объясняет загрузку процессора через число активных работ и честно сообщает, если сведений о занятых слотах нет.
- Evidence: целевой Vitest → 4 passed; полный Vitest → 78 passed; TypeScript, ESLint, Vite build, `go test ./...`, `go build ./...` → passed.
- Next action: открыть `/work` с повторно запущенной стадией и обзор `/` для визуальной приёмки.

## LOG

### 2026-08-08 — Implement

На свежем `origin/main` перенесена только логика CARD-0031 без 60 чужих файлов старой custom-ветки. Повтор определяется по истории одноимённых автоматических задач и относится к живой стадии. Добавлено честное пояснение CPU и четыре регрессионных теста; целевые и полные frontend-проверки, Go suite и обе сборки прошли.
