Implementation commit: 76d16c5191dcc8c44a001ffb71dbbaebf183f573 — PR #197 доставил безопасную Gate-цепочку в основную ветку.

# CARD-0106 — Исправить учёт после слияния CARD-0083

## HEAD

- Status: Backlog — отдельная учётная работа, продуктовая разработка не нужна.
- What changed: зафиксировано, что повторная работа Gate закрыта как дубликат уже
  влитого PR #197; обнаруженные ошибки карточек отделены от launcher-кода.
- Evidence: свежий `main` содержит implementation commit PR #197, но
  `CARD-0083-real-session-before-gate.md` всё ещё имеет статус
  `Verified PASS — awaiting human merge`; номер CARD-0083 также использует
  карточка Settings.
- One next action: отдельным документальным изменением выбрать канонические
  номера, обновить статусы обеих доставленных работ и сохранить перенаправления
  для существующих ссылок.

## Контракт учётной работы

Исправить только метаданные двух карточек:

- `knowledge/cards/CARD-0083-real-session-before-gate.md` должен сообщать, что
  Gate доставлен в `main` через PR #197, а не ожидает merge;
- `knowledge/cards/CARD-0083-settings-save-without-first-verify.md` не должен
  делить тот же канонический номер с Gate;
- перед переименованием найти ссылки на оба пути и сохранить понятное
  перенаправление либо одновременно обновить все ссылки;
- не менять `ops/fx-factory-release`, его тесты или другое продуктовое поведение.

## Причина отдельной карточки

Владелец решил закрыть текущую задачу как уже выполненную / дубликат. Ошибки
статуса и номера не означают регрессию Gate и не должны запускать четвёртую
разработку поверх тех же launcher-файлов.

## LOG

### 2026-08-12 — Specification

Свежий `origin/main` и опубликованные ветки проверены перед выбором номера:
`CARD-0106` не был занят. Учётный долг отделён от закрытия доставленного PR #197.
