# CARD-0051 — Канал уведомлений владельца в задачах Пилота

## HEAD

- Status: done
- Branch: `factory/34261776-b20-2179df0f-15f`
- Head commit: `1bd5643`
- What changed: Пилот добавляет в правила автоматической задачи URL срочных
  уведомлений, собранный из `ntfy_server` и `ntfy_owner_topic`. При длинном
  исходном контексте правила и канал сохраняются в пределах лимита 60 000 знаков.
- Evidence: `AgentRulesScopeTests` — 5/5; весь `pilot.test_pilot` — 102/102;
  `npx tsc -p tsconfig.app.json --noEmit` и `npm run build` — PASS.
- Next action: на Review проверить, что в контекст не попадает канал при
  отсутствующей теме владельца.

## LOG

### 2026-08-10 — Implement

Статические правила автоматических задач заменены конфигурационно-зависимой
сборкой: заполненная тема владельца превращается в безопасный URL ntfy и передаётся
локальному воркеру. Неполная настройка не создаёт ложный адрес, а обрезание длинного
контекста оставляет весь блок правил. Подтверждено 5 целевыми и 102 полными тестами
Пилота, компиляцией Python, проверкой TypeScript и production-сборкой web.
