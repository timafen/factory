# CARD-0128 — Денежный сторож вмешивается после пятого круга

Implementation commit: b122caf5207ed979baeaf266b9928d6b8c503aa9 — ранняя диагностика независимо обрабатывает одноимённые работы по устойчивому `work_id`

## HEAD

Status: Implemented.
Branch: `factory/e3b88b4f-884-fbeaf472-ab7`.
Implementation commit: b122caf5207ed979baeaf266b9928d6b8c503aa9 — обход и DIAG-маркер различают работы по `work_id`.
What changed: `diag_sweep()` считает круги, дедуплицирует живые задачи и хранит факт ранней диагностики по устойчивому `work_id`. Две активные работы с одинаковым заголовком больше не блокируют диагностику друг друга.
Evidence: `python3 -m unittest pilot.test_pilot.DiagnosisRepairTests -v` — 21/21, `OK`; `python3 -m py_compile pilot/pilot.py pilot/test_pilot.py` — код 0. Полный `pilot.test_pilot` — 258 тестов, 2 известных сбоя вне области в `CorrectionProvenanceStormTests`.
One next action: провести Review поставки относительно свежей основной ветки.

## LOG

### 2026-08-13 — Implement

Исправлено сопоставление ранней диагностики с современными задачами: вместо владельческого заголовка счётчик получает задачу с устойчивым `work_id`.
Добавлены регрессии на срабатывание ровно на пятом круге и изоляцию одноимённых работ; 19 целевых сценариев прошли, синтаксическая компиляция завершилась с кодом 0.

### 2026-08-13 — Implement

Устранена оставшаяся коллизия одноимённых работ: ключи обхода и DIAG-маркера переведены с заголовка на `work_id`.
Новый сценарий подтверждает, что при двух живых одноимённых работах сторож диагностирует вторую, если только она достигла пятого круга; все 21 целевая проверка прошла.
Полный модуль выполнил 258 тестов и сохранил два существующих сбоя вне области в сценариях перезапуска `CorrectionProvenanceStormTests`.
