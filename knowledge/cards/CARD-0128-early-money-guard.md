# CARD-0128 — Денежный сторож вмешивается после пятого круга

Implementation commit: 6f3f50a3ff53fb32b4c5133c1c11266187e08bd9 — ранняя диагностика считает современные работы по устойчивому `work_id`

## HEAD

Status: Implemented.
Branch: `factory/ce52da56-173-f30efb95-d5e`.
Implementation commit: 6f3f50a3ff53fb32b4c5133c1c11266187e08bd9 — сторож разрывает цикл современной работы на пятом круге.
What changed: `diag_sweep()` передаёт полную живую задачу в счётчик кругов, поэтому задачи с `work_id` больше не выглядят как работы с нулём попыток. Одноимённые независимые работы не смешиваются при подсчёте.
Evidence: `python3 -m unittest pilot.test_pilot.DiagnosisRepairTests -v` — 19/19, `OK`; `python3 -m py_compile pilot/pilot.py pilot/test_pilot.py` — код 0.
One next action: провести Review поставки относительно свежей основной ветки.

## LOG

### 2026-08-13 — Implement

Исправлено сопоставление ранней диагностики с современными задачами: вместо владельческого заголовка счётчик получает задачу с устойчивым `work_id`.
Добавлены регрессии на срабатывание ровно на пятом круге и изоляцию одноимённых работ; 19 целевых сценариев прошли, синтаксическая компиляция завершилась с кодом 0.
