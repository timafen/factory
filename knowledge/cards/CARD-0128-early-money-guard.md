# CARD-0128 — Денежный сторож вмешивается после пятого круга

Implementation commit: 6c50be764109e97962e5e2930ca7f9f829bd395c — старшая диагностика разбирает застрявшую работу после пятого круга

## HEAD

Status: Implemented — guard уже есть в свежей `origin/main`.
Branch: `factory/1861c5d7-4fb-d3e91e3f-f55`.
Implementation commit: 6c50be764109e97962e5e2930ca7f9f829bd395c — диагностика после пяти кругов допускает один безопасный ремонт или зовёт владельца.
What changed: `diag_sweep()` вызывается в основном цикле независимо от вопросов pipeline; владельческий диагноз ставит работу на паузу и создаёт вопрос.
Evidence: `deep_diag_rounds = 5`; `python3 -m unittest pilot.test_pilot.DiagnosisRepairTests -v` — 17/17, `OK`, код 0.
One next action: закрыть pipeline как уже выполненный.

## LOG

### 2026-08-13 — Implement

Свежая `origin/main` уже содержит требуемый ранний денежный guard: на пятом круге вызывается одноразовая старшая диагностика, технический диагноз запускает один безопасный ремонт, а владельческий останавливает работу и сохраняет содержательный вопрос.
Рабочая конфигурация подтверждена чтением `/opt/factory-data/pilot/config.json`: `deep_diag_rounds = 5`.
Целевой класс `DiagnosisRepairTests` завершился с результатом 17/17 и кодом 0; новых продуктовых изменений не требуется.
