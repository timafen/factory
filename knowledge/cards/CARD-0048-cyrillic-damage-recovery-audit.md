# CARD-0048 — Аудит повреждённой кириллицы без изменения данных

## HEAD

- Status: Verified PASS — awaiting human merge.
- Branch: `factory/25f8a66a-2f6-c9f1b31b-368`.
- Head commit: `02d7be3` — аудит принимает полный commit SHA в любом регистре.
- Evidence: чистый полный `go test ./... -count=1` (5 пакетов), целевые проверки
  неизменности SQLite/Git, защита от сети и подмены Git-объектов, `go vet`, сборка
  и `git diff --check` → PASS.
- One next action: выполнить человеческое слияние в `main`.

## LOG

### 2026-08-10 — Specification

Восстановление запрещено без отдельного решения владельца. Контракт ограничен
офлайн-инвентаризацией согласованного SQLite snapshot и явно перечисленных
Git-коммитов. Исследуемые тексты допустимы во входном файле, но не в отчёте;
один `?` сам по себе не считается доказательством исходного символа.

### 2026-08-10 — Implement

Добавлена офлайн-команда `factory-audit-cyrillic`. SQLite открывается через
`mode=ro&immutable=1`, Git читается через `git show` с отключёнными optional
locks, а отчёт содержит только идентификаторы, счётчики и статусы сопоставления.
Регрессия подтверждает неизменность байтов snapshot и состояния Git, отсутствие
WAL/SHM и отсутствие обоих исследуемых текстов в JSON. Целевые тесты, полный
Go-набор, сборка команды, `go vet` и `git diff --check` прошли.

### 2026-08-10 — Implement

После REQUEST CHANGES вызов Git принудительно получил `GIT_NO_LAZY_FETCH=1` и
`GIT_NO_REPLACE_OBJECTS=1`. Новые тесты подтверждают, что отсутствующий объект
не скачивается из promisor remote и что `refs/replace` не подменяет заголовок.
Обе регрессии, полный набор из 13 Go-пакетов, сборка и целевой `go vet` прошли.

### 2026-08-10 — Implement

Поставка заново собрана на свежем `origin/main` только из шести файлов задачи.
Перед чтением заголовка Git теперь подтверждает тип `commit`, а возвращённый
`%H` обязан совпасть с входным полным SHA. Регрессии с SHA дерева и blob,
целевые тесты, обязательный `internal/worker`, сборка и `go vet` прошли.

### 2026-08-10 — Implement

После REQUEST CHANGES сравнение полного SHA переведено на `strings.EqualFold`.
Регрессионный тест подтверждает чтение корректного коммита по SHA в верхнем
регистре; проверки типа объекта, целевые тесты, обязательный тест воркера,
сборка, `go vet` и `git diff --check` прошли.

### 2026-08-10 — Verify

| Критерий | Проверка | Наблюдение |
| --- | --- | --- |
| SQLite только для чтения и только повреждённые заголовки | `TestAuditInventoriesWithoutChangingOrDisclosingInputs` | Найдены три повреждённые записи; digest и отсутствие WAL/SHM сохранены. |
| Git только для указанного полного commit SHA | целевые `TestGitSubject*` | Верхний регистр SHA принят; tree/blob, lazy-fetch и replace refs отклонены или проигнорированы. |
| Сопоставление не выдаёт догадки за восстановление | `TestReadSourcesRejectsUnverifiableInput`, `TestCompareDoesNotTreatExistingQuestionMarkAsEvidence` | Неоднозначные/неполные источники отклонены, исходный `?` не служит доказательством. |
| JSON не раскрывает исследуемые тексты, данные не меняются | `TestAuditInventoriesWithoutChangingOrDisclosingInputs` | Проверка прошла; отчёт содержит только безопасные метаданные. |
| Полная регрессия поставки | `go clean -testcache && go test ./... -count=1` | Все 5 пакетов прошли. |
