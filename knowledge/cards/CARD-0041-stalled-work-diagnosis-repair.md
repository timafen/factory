# CARD-0041 — Разбор застрявшей работы чинит технический цикл

## HEAD

- Status: Implemented — блокеры Review исправлены, ожидает повторный Review.
- Branch: `factory/c9164596-7b6-412172ce-eb9`.
- Head commit: `416dc30` (реализация и регрессионная проверка).
- Specification: `knowledge/specs/stalled-work-diagnosis-repair.md`.
- What changed: терминальный сбой из реального маршрута `cycle()` запускает
  одноразовое продолжение с найденным исправлением; актуальный учёт расходов
  Codex и его тесты сохранены без изменений.
- Evidence: целевые проверки → 11 tests OK; весь `pilot.test_pilot` → 58 tests OK;
  `py_compile` и `git diff --check` → OK.
- One next action: проверить реализацию на Review.

## LOG

### 2026-08-09 — Implement

Восстановлен утверждённый контракт и реализован устойчивый автоматический ремонт:
Фабрика адресно отменяет единственный технически зациклившийся запуск, ждёт его
терминального состояния и один раз продолжает исходную стадию с найденным решением
и прежней веткой. Шесть целевых и все 42 теста пилота прошли; синтаксис и пробелы
проверены. Повторная отмена, повторное продолжение и воздействие на неоднозначные
активные запуски закрыты тестами.

### 2026-08-09 — Implement

Закрыты замечания Review о двух границах перезапуска и ограниченном списке задач.
Сохранённые `cancel_pending` и `resume_pending` теперь восстанавливаются
идемпотентным повтором того же действия, а отсутствующая в снимке исходная задача
читается по сохранённому ID. Девять целевых и все 45 тестов пилота прошли;
синтаксис и чистота diff проверены.

### 2026-08-09 — Implement

Перед автоматической отменой добавлен полный курсорный обход задач без ограничения
первой сотней. Регрессионный тест помещает второй активный запуск на следующую
страницу и подтверждает, что ни один запуск не отменяется. Все 10 сценариев
автопочинки и все 46 тестов пилота прошли; синтаксис и чистота diff проверены.

### 2026-08-09 — Implement

Исправлен блокер Review: повторный сбой, попавший в `route_question()`, теперь
передаёт единственный активный запуск в диагноз и сразу начинает устойчивый
ремонт, а не оставляет уже израсходованный лимит `DIAG`. Регрессионный сценарий
через `route_question()` и все 47 тестов пилота прошли; синтаксис и diff проверены.

### 2026-08-09 — Implement

Работа заново собрана от свежего `origin/main` без посторонних файлов. Устранён
блокер `pilot/pilot.py:2196`: при `cap_rescues(..., "DIAG") == 1` повторный
технический сбой из `route_question()` доходит до ремонта ровно один раз, не
увеличивая счётчик. Все 11 целевых и 47 тестов пилота прошли; синтаксис и чистота
diff подтверждены.

### 2026-08-09 — Implement

Работа повторно собрана от свежего `origin/main` без регрессии учёта расходов
Codex. Ремонт теперь запускается из терминальной ветки `cycle()`: после повторного
сбоя и исчерпанного `DIAG` создаётся одна задача с решением и прежней веткой.
Все 11 сценариев ремонта и все 58 тестов пилота прошли; `py_compile` и проверка
diff завершились успешно.
