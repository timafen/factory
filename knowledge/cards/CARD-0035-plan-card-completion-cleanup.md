# CARD-0035 — Завершённые работы сами уходят из открытого Плана

## HEAD

- Status: done
- Branch: `factory/9ff6bebb-0d2-fa36ba5e-26c`
- Head commit: `c821b4f` (проверенный снимок до итогового squash-коммита)
- What changed: перед новым автоподбором План переводит связанную карточку в
  `done`, только когда последний этап прогона завершён и строго принят.
- Evidence: целевые 5 и все 12 тестов `pilot.test_pilot` прошли; Go-тесты и
  сборка прошли; TypeScript-проверка прошла. Две несвязанные ошибки web-тестов
  `Overview` и `Settings` зафиксированы в рисках.
- Next action: выпустить обновлённый `main` и проверить очередной цикл Плана.

## LOG

### 2026-08-09 — Implement

Проверенный снимок перенесён на свежий `origin/main` и подготовлен к поставке
одним squash-коммитом. `python3 -m unittest pilot.test_pilot` повторно прошёл:
12 тестов, включая все пять сценариев очистки карточек.

### 2026-08-08 — Specification

Зафиксированы связь через `task_id`, временная граница прогона, строгий финальный
вердикт и запрет повторного автозапуска закрытой карточки в том же цикле.

### 2026-08-08 — Implement

Добавлена сверка открытых карточек со снимком задач до раздачи новой работы.
Пять целевых сценариев подтверждают успешное закрытие и защиту от старого
прогона, отсутствующего `task_id`, неуспеха, отмены и открытого вопроса.
Полный модуль pilot, Go-тесты, Go-сборка и web-сборка прошли успешно. В общем
web-наборе остались 4 не связанные с этой поставкой базовые ошибки из 98 тестов.

### 2026-08-09 — Verify

| Критерий | Проверка | Результат |
| --- | --- | --- |
| Успешный финал закрывает связанную карточку | `PlanCardCleanupTest` | Карточка `in_work` переведена в `done`; строгий `final_ok` вызван для финального этапа. |
| Закрытая planned-карточка не стартует вновь | `PlanCardCleanupTest` | Состояние меняется до отбора кандидатов, повторный запуск исключён. |
| Неполный, нестрогий, упавший, отменённый или ожидающий ответ прогон остаётся открыт | `PlanCardCleanupTest` | Все пять защитных вариантов не меняют карточку. |
| Старый прогон не закрывает новую карточку | `PlanCardCleanupTest` | Прогон раньше связанного `task_id` не принят. |
| Карточка без `task_id` не закрывается по заголовку | `PlanCardCleanupTest` | Неоднозначная карточка сохранена открытой. |

`python3 -m unittest pilot.test_pilot` — 12 tests, OK; `go test ./...` и
`go build ./...` — успешно; `npx tsc -p tsconfig.app.json --noEmit` — успешно.
`npm test` обнаружил две ранее существующие, не затронутые этой работой ошибки:
`Overview active work` и `Settings`.
