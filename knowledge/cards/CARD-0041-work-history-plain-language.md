# CARD-0041 — История работы простыми словами

## HEAD

- Статус: Specification — ожидает реализации.
- Ветка спецификации: `factory/725dd166-813-8a598faa-027`.
- Цель: заменить техническую простыню событий на странице задачи короткой русской историей действий, не меняя API и хранение событий.
- Спецификация: `knowledge/specs/work-history-plain-language.md`.
- Область реализации: `web/src/format.ts`, `web/src/format.test.ts`.
- Обязательная проверка реализации: `cd web && npm test -- --run src/format.test.ts`.
- Следующее действие: Implement обновляет форматтер и целевые тесты по критериям спецификации, затем дополняет эту карточку доказательствами.

## LOG

### 2026-08-09 — Specification

По текущему коду подтверждено: `TaskDetail` уже отделён от протоколов runtime функцией `eventSummary()`, поэтому задача решается локально в форматтере и его тестах. Сервер, API и компонент страницы менять не требуется. Исходная ветка принятой спецификации `factory/9b019e5b-fc7-3d848040-2d6` отсутствовала на `origin`; спецификация восстановлена из фактического поведения `main` и дополнена обязательными проверяемыми обещаниями.
