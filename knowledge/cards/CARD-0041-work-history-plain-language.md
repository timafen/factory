# CARD-0041 — История работы простыми словами

## HEAD

- Статус: Implement + Test — реализовано, ожидает проверки.
- Ветка: `factory/72973238-83b-a30b54b7-9c8`.
- Head commit: `f2e6d2c`.
- Что изменилось: история задачи показывает короткие русские записи о сообщениях, командах, инструментах, файлах и ошибках.
- Что скрыто: сырой stdout/stderr, payload инструментов, служебные ID, reasoning, lifecycle, telemetry и неизвестные объектные события.
- Evidence: целевой тест → 12 passed; typecheck и lint → passed; production build → passed (1735 modules).
- Следующее действие: Verify проверяет целевой тест и отображение истории на странице задачи.

## LOG

### 2026-08-09 — Specification

По текущему коду подтверждено: `TaskDetail` уже отделён от протоколов runtime функцией `eventSummary()`, поэтому задача решается локально в форматтере и его тестах. Сервер, API и компонент страницы менять не требуется. Исходная ветка принятой спецификации отсутствовала на `origin`; спецификация восстановлена из фактического поведения `main` и дополнена обязательными проверяемыми обещаниями.

### 2026-08-09 — Implement

Форматтер переведён на короткие русские сообщения для событий Codex и Claude Code. Сырой вывод, внутренние payload и служебные события исключены из истории; сообщения исполнителя и структурированные ошибки сохранены. Целевой набор прошёл 12 проверок, typecheck, lint и production build завершились без ошибок.
