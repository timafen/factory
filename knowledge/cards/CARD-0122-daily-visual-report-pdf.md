# CARD-0122 — Ежедневный визуальный отчёт PDF

Implementation commit: 270854f9d720299b94411f78f6fb310cabf48d17 — опубликованы PDF, снимки «до/после», метрики, история и экран `/reports`

## Состояние

- Реализация опубликована в `origin/factory/28591ece-f5d-5baef51e-932`.
- Specification фиксирует продолжение после дубликата, без новых изменений
  продукта или UI.
- Остались две проверки: исправить повторный `Store.Close` в cleanup-тесте и
  пройти живой `/reports` системным Chromium. До их PASS карточка BLOCKED.

## Проверяемый результат

Отчёт создаётся один раз на день и timezone, хранит PDF с SHA-256, показывает
состояния снимков и метрики, а API безопасно обрабатывает ошибки БД и URL.
Экран `/reports` даёт владельцу историю и скачивание готового PDF.

## Следующее действие

Сделать cleanup теста ошибки БД идемпотентным, выполнить обязательную команду
из спецификации пять раз и затем проверить живой экран `/reports` Chromium.
