# CARD-0038 — Синхронизировать конфиг пилота со схемой сервера

## HEAD

- Status: Implemented — сниженные лимиты сохранены в проекте, проверки зелёные.
- Branch: `factory/05a55a28-fa7-450b1e38-a24`.
- Head commit: `ec5c387` (ревизия реализации до обновления карточки).
- What changed: эталонный конфиг пилота задаёт не более 12 задач на одну
  работу за сутки и не более 120 задач суммарно; тест закрепляет оба значения.
- Evidence: целевой Go-тест и 32 теста пилота завершились успешно.
- Next action: проверить поставку, слить в `main`, затем выпустить штатной
  командой `sudo -n /usr/local/bin/fx factory release` и сверить живые лимиты.

ГОТОВО-КОГДА: файл internal/controlplane/pilot_config_test.go
ГОТОВО-КОГДА: файл pilot/config.example.json
ГОТОВО-КОГДА: команда go test ./internal/controlplane -run TestPilotConfigExampleMatchesServerSchema -count=1

## LOG

### 2026-08-09 — Specification

Зафиксирован минимальный контракт: добавить уже используемый пилотом флаг в
строгую серверную модель, сохранить совместимый default `true` и поставить
регрессионные ворота на полном репозиторном примере конфигурации. Исходная ветка
принятой спецификации была удалена с `origin`, поэтому документ восстановлен по
актуальному коду на свежем `origin/main`.

### 2026-08-09 — Implement

В строгую модель сервера и эталонный конфиг добавлен `respect_host_load`.
Совместимость старых файлов и сохранение явного `false` закреплены тестом на
полном `pilot/config.example.json`; целевой тест, `go test ./...` и
`go build ./...` завершились успешно. Проверенная ревизия реализации: `2e23a41`.

### 2026-08-09 — Verify

| Критерий | Проверка | Результат |
| --- | --- | --- |
| Старый файл получает `true` | `TestPilotConfigExampleMatchesServerSchema` | принят и возвращает `respect_host_load: true` |
| Явное `false` сохраняется | тот же тест: `Write` → `Read` | значение и JSON-ключ сохранены |
| Эталон проходит строгую схему | тот же тест читает `pilot/config.example.json` | файл принят `DisallowUnknownFields`-декодером |
| Неизвестные поля отклоняются | тот же тест добавляет неизвестный ключ | чтение отвергнуто |
| Новое несхемное поле ломает ворота | строгий декодер эталона в том же тесте | неизвестный JSON-ключ вызывает ошибку |

Дополнительно выполнены `go clean -testcache && go test ./...` и `go build ./...`:
все тестируемые пакеты завершились `ok`, сборка завершилась с кодом 0. Дифф от
точки ветвления содержит только шесть заявленных файлов; `git diff --check`
не нашёл ошибок пробелов.

### 2026-08-09 — Implement

После разгрузки потолки 12/120 добавлены в отслеживаемый
`pilot/config.example.json`, чтобы штатный релиз больше не зависел от ручной
правки живого файла. Регрессионный тест проверяет наличие точных JSON-значений
и их чтение серверной схемой; целевой Go-тест и 32 теста пилота прошли.
