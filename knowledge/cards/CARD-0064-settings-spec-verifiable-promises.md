# Проверяемые обещания спецификации экрана «Настройки»

## HEAD

Status: Implemented, checks green
Branch: `factory/d47be451-5cf-41457df2-9aa`
Implementation commit: 99f023ca39ef5d5153f5e16e670db4692cf6c030 — экран показывает и сохраняет все рабочие ограничения пилота, а атомарная запись не теряет ошибки.

Что изменилось: `/settings` получил нагрузку хоста, лимиты кругов, спасений,
дня и диагностики, а также группы уведомлений диагностики и Плана. Конфликт
версий объясняется владельцу по-русски; неудачная временная запись сохраняет
прежний конфиг.

Evidence: целевой `go test` → PASS; `Settings.test.tsx` → 9 PASS;
Playwright `/settings` → 1 PASS; TypeScript, ESLint, Vite и Go build → PASS.

One next action: проверить `/settings` после выпуска ветки в окружение владельца.

## LOG

### 2026-08-11 — Implement

- Полная действующая схема пилота доступна на `/settings` и сохраняется без
  потери новых операционных полей и групп уведомлений.
- Ошибка записи временного файла теперь останавливает atomic rename; исходный
  `config.json` остаётся без изменений.
- Доказательство: 9 unit-тестов интерфейса, целевые Go-тесты и браузерный
  сценарий сохранения с перезагрузкой прошли.

### 2026-08-11 — Specification

Спецификация `docs/software-factory/settings-spec.md` дополнена списком файлов
реализации и воспроизводимой целевой командой проверки. Обещания связали экран
`/settings`, API и атомарное хранение настроек с проверяемым результатом.

Границы этапа: менялись только документация спецификации и отдельная карточка;
реализация экрана, API и тестов оставалась в предыдущем кодовом коммите.
