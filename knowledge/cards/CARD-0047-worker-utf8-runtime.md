# CARD-0047 — UTF-8 для текста задач и коммитов воркера

## HEAD

- Status: Specification — ожидает реализации.
- Branch: `factory/a9e7cffa-306-8702dc68-90b`.
- What changed: зафиксирован план явного UTF-8-окружения для runtime-процесса
  воркера и регрессия, воспроизводящая кириллические заголовок задачи и текст
  коммита при исходной ASCII-locale.
- Evidence: код просмотра показал, что `superviseRuntime` запускает Codex или
  Claude Code без `Cmd.Env`, то есть с locale родительского процесса.
- One next action: Implement добавляет общий UTF-8 environment и целевой тест.

## LOG

### 2026-08-10 — Specification

Исходная ветка принятой спецификации `factory/6a183d47-b4b-ca2ee1bf-d1f`
больше не доступна на `origin`, поэтому документ восстановлен по актуальному
коду на свежем `main`. Область намеренно ограничена запуском runtime-процесса:
форматы задач, HTTP API, база данных и интерфейс уже передают строки как UTF-8
и не требуют изменений.
