# CARD-0031 — Принятая работа: блок «Что изменилось» вместо пустоты

## HEAD

- Status: implemented
- Branch: `factory/5892f990-b32-0a9a71d7-a44`
- Head commit: `210434a`
- What changed: Страница принятой задачи показывает человеческую часть Verify-отчёта; если её нет — последний `Attempt.result`.
- What changed: Строго известные служебные строки скрываются, а полностью служебный отчёт заменяется понятной заглушкой.
- Evidence: `npm test -- --run src/Summary.test.tsx` → 4/4 passed; targeted eslint → passed; `npm run build` → passed.
- Next action: открыть завершённую задачу в `/tasks/<task-id>` и визуально подтвердить блок на реальном отчёте.

## LOG

### 2026-08-08 — Specification

Зафиксированы источники блока: полный verdict этапа Verify и последний `Attempt.result` той же задачи. Согласована консервативная фильтрация: неизвестные строки остаются видимыми, блок никогда не остаётся пустым.

### 2026-08-08 — Implement

Добавлен блок «Что изменилось» с приоритетом Verify-verdict, запасным `Attempt.result` и централизованным фильтром полей `BRANCH`, `HEAD`, `PUSHED`, `TRY` и отдельных машинных verdict-строк. Четыре теста покрывают оба источника, пустой остаток и сохранение неизвестных строк; production-сборка прошла. Общие test/lint остаются красными на ранее существующих ошибках вне изменённых файлов.
