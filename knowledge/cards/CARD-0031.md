# CARD-0031 — Принятая работа: блок «Что изменилось» вместо пустоты

## HEAD

- Status: Implemented — чисто перенесено на актуальный `main`, готово к ревью.
- Branch: `factory/37777e21-a1a-58584fc1-4ca`.
- Head commit: `31d62f0` (до записи результатов чистого переноса).
- Что изменилось: страница принятой задачи показывает человеческую часть Verify-отчёта; если её нет — результат последней попытки.
- Что изменилось: известные служебные строки скрываются, полностью служебный отчёт заменяется понятной заглушкой.
- Evidence: TypeScript → PASS; целевые тесты → 4/4 PASS; eslint изменённых файлов → PASS; production build → PASS; diff к `origin/main` → только 7 файлов CARD-0031.
- One next action: открыть принятую задачу в `/tasks/<task-id>` и подтвердить блок на реальном отчёте.

## LOG

### 2026-08-08 — Specification

Источники блока: полный verdict этапа Verify и последний `Attempt.result` той же задачи. Фильтрация консервативная: неизвестные строки остаются видимыми, блок с имеющимся отчётом никогда не остаётся пустым.

### 2026-08-08 — Implement

Добавлен блок «Что изменилось» с приоритетом Verify-verdict, запасным `Attempt.result` и централизованным фильтром полей `BRANCH`, `HEAD`, `PUSHED`, `TRY` и отдельных машинных verdict-строк. Четыре целевых теста, TypeScript, eslint изменённых файлов и production-сборка прошли.

Полный UI-прогон выявил существующие вне этой поставки проблемы: 15 тестов `App.test.tsx` и 11 общих eslint-ошибок. Изменённые файлы этих ошибок не содержат; исправлять их в рамках CARD-0031 нельзя без расширения задачи.

### 2026-08-08 — Implement

Два принятых коммита CARD-0031 перенесены на назначенную ветку от актуального `origin/main`; посторонние изменения API статуса, Settings, экрана «Работа», навигации и CARD-0030 исключены. Целевые тесты 4/4, TypeScript, eslint трёх изменённых исходников и production build прошли; diff содержит только 7 файлов этой задачи. Полный Vitest: 71 passed и 15 известных падений в неизменённом `App.test.tsx`; `go test ./...` повторяет базовую несовместимость типов в неизменённом `internal/controlplane/pilot_config_test.go`.
