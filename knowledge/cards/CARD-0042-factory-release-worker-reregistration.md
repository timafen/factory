# CARD-0042 — Выкат Фабрики возвращает воркера в строй

## HEAD

- Status: Specification — ожидает реализации.
- Branch: `factory/141ffd0f-c86-968b4947-a36`.
- Specification: `knowledge/specs/factory-release-worker-reregistration.md`.
- Goal: выкат обновляет сервер и воркер одной ревизией и подтверждает повторную
  здоровую регистрацию воркера до сообщения об успехе.
- Implementation scope: `ops/fx-factory-release`,
  `ops/test-fx-factory-release.sh`.
- One next action: подтвердить production-имя службы и путь бинаря воркера, затем
  реализовать спецификацию и целевой shell-тест.

## LOG

### 2026-08-09 — Specification

По действующему сценарию выката зафиксирован разрыв: обновляется и перезапускается
только сервер. Согласована минимальная поставка — обновлять пару бинарей,
перезапускать воркер после готовности сервера, проверять его регистрацию и при
сбое откатывать оба компонента.
