# CARD-0053 — Завершение подзадач эпика переживает очистку истории

## HEAD

- Status: Verified PASS — готово к слиянию.
- Branch: `factory/f038362a-a18-27b85255-e35`.
- Head commit: текущий HEAD этой ветки; фактический идентификатор задаёт Git, а не устаревающий хеш в карточке.
- What changed: Пилот сохраняет квитанцию завершения и восстанавливает running-подзадачу только по точному merge после её `started_at`.
  Перезапуск с `failed` или `stuck` фиксирует границу по новейшей одноимённой задаче, поэтому очистка истории не применяет старую квитанцию.
- Evidence: `python3 -m unittest pilot.test_pilot` → 115 tests OK; `just build` → OK.
- One next action: влить проверенную ветку в `main`.

## LOG

### 2026-08-10 — Implement

Перенесена целевая реализация из полной ветки `factory/be2db5ef-76a-dcdec47d-671`
на свежий `origin/main`; общий backlog CARD-0030 не изменялся. Регрессионные тесты
покрывают сохранение квитанции после очистки, точное и свежее совпадение merge,
запрет старого merge, `hold_reason`, новое поколение и сохранение `parallel_ok`.
`python3 -m unittest pilot.test_pilot` завершился 114 проверками OK; `just build` прошёл.

### 2026-08-10 — Implement

Исправлена граница поколения при перезапуске: самая новая одноимённая задача
задаёт `started_at` независимо от состояния, включая `failed` и `stuck`.
Регрессия очищает историю после обоих состояний и подтверждает, что старый merge
не завершает подзадачу. `python3 -m unittest pilot.test_pilot` — 115 OK; `just build` — OK.
