# CARD-0131 — «Сделано недавно»: влитое отдельно и даты по-человечески

Implementation commit: ec8fbc50fbd5d3c74214e5e7db15b6c1cab5a4b5 — влитые и остановленные работы показаны отдельно, служебные данные скрыты.

## HEAD

- Status: Implemented.
- Branch: `factory/0123257c-5cc-24272e3a-912`.
- Implementation commit: ec8fbc50fbd5d3c74214e5e7db15b6c1cab5a4b5 — влитые и остановленные работы показаны отдельно, служебные данные скрыты.
- What changed: обзор разделяет влитые и остановленные работы, подписывает даты, этапы и причины по-человечески.
- What changed: отдельные SHA длиной 7–40 символов, включая `e627e82`, не попадают в причину остановки.
- Evidence: `python3 -m unittest -v pilot.test_pilot.RecentDoneTest` → 8 tests OK; Overview → 30 tests passed.
- Evidence: `npm run typecheck`, ESLint и `npm run build` → passed.
- Next action: повторно запустить Review для опубликованной ветки-кандидата.

## LOG

### 2026-08-13 — Implement

Серверный контракт разделён на подтверждённые receipt-слияния и `failed`/`cancelled`
остановки с независимым лимитом пяти записей. Интерфейс показывает человеческие
даты, этап и причину, а контрактная и UI-регрессии подтверждают, что пять новых
провалов не скрывают четыре влитые работы.

### 2026-08-13 — Implement

Причина остановки теперь извлекается только из безопасной помеченной строки;
сырой результат с task_id, SHA или PID заменяется нейтральным текстом.
Регрессионный тест подтверждает отсутствие этих данных; целевые и frontend/Go
проверки зелёные, в полном Python-наборе остаются две прежние ошибки восстановления.

### 2026-08-13 — Implement

Очистка причины расширена на отдельные короткие SHA: значение `e627e82` теперь
заменяется нейтральной причиной. Отдельная регрессия и все 8 тестов блока
`RecentDoneTest` зелёные; 30 UI-тестов, typecheck, ESLint и сборка также прошли.
