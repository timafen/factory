Implementation commit: ce1c7fb9ec44f19959167b50a9f12c99968d6ddd — определена проверяемая граница живого парка и архива исполнителей

# CARD-0174: Живой парк исполнителей и архив регистраций

## HEAD

Status: Specified
Branch: factory/93f9a816-385-f040f766-fe1
Specification: `knowledge/specs/live-worker-fleet-archive.md`
What changes: экран отделяет регистрации с heartbeat младше 7 суток от
архива, не меняя 30-секундный статус online и не удаляя серверные записи.
Evidence required: `cd web && npm test -- Workers.test.tsx` проверяет точную
семидневную границу и возврат исполнителя после нового heartbeat.
One next action: Implement + Test реализует описанное web-представление и
целевой компонентный тест.

## LOG

### 2026-08-15 — Specification

Владелец утвердил архивирование через 7 суток после `last_heartbeat`.
Исполнитель остаётся в основном списке со статусом `Offline` после прежнего
30-секундного окна и вплоть до семидневного порога. Начиная с порога он
показывается в отдельном закрытом архиве.

Архив определён только как представление `WorkersView`: `GET /api/v1/workers`,
SQLite и общий query cache продолжают содержать все регистрации. Поэтому новый
heartbeat возвращает строку в основной парк при следующем 10-секундном
обновлении, а архивная строка сохраняет переход в detail.

Сводка считается по текущему парку и отдельно показывает размер архива.
Зафиксированы состояния пустого и полностью архивного парка, точная граница,
консервативное поведение для невалидной даты и адаптивность на 390 px.

Номер `CARD-0174` выбран после проверки свежего `origin/main` и 1272
опубликованных remote refs: карточек `CARD-0174-*` и точного пути этой работы
нет.
