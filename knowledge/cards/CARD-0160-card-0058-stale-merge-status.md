# CARD-0160 — Устаревший merge-статус CARD-0058

Implementation commit: 6e1e351f83de1de5f4d057f3a711f8f58684d55d — решение перехода сохраняется во время ожидания области и ворот Review и переиспользуется после их освобождения.

## Статус

- Type: documentation debt.
- State: proposed; не входит в текущую дублирующую продуктовую работу.
- Source: `knowledge/cards/CARD-0058-pilot-overlap-wait-decision-cache.md`.

## Проблема

Раздел `HEAD` исходной карточки по-прежнему говорит `awaiting human merge` и
предлагает выполнить merge. Это уже неверно: реализация вошла в `main` как
«Ожидающая пересечения работа не вызывает модель заново каждые полминуты», а
арбитраж queued-работ позднее исключил взаимное ожидание.

## Граница будущей работы

Обновить в `CARD-0058` только текущий статус, словесную подпись поставки и
следующее действие. Сохранить Implementation commit, LOG и свидетельства тестов;
не менять `pilot/pilot.py`, не повторять merge и не запускать выкат.

## Приёмка долга

- `CARD-0058` однозначно сообщает, что поведение уже поставлено в `main`.
- В карточке нет действия «выполнить human merge».
- История реализации и результаты целевых проверок остаются сохранены.
