Implementation commit: e941d725b099e16bc16e633d9b8983189c95cf69 — ранее реализована изоляция попыток одноимённых работ по `work_id`; это фундамент, а не реализация данной спецификации.

# CARD-0164: снимок задач для `stage_attempts()`

## HEAD

Status: Specified — awaiting Implement
Branch: factory/a3004d79-c15-e473a3c6-b52
What changed: определён контракт подсчёта попыток только по переданному снимку
задач и регрессионный сценарий появления новой задачи после снимка.
Evidence: спецификация сопоставлена с текущими `stage_attempts()`, `cycle()` и
тестами изоляции работ в `pilot/test_pilot.py`.
One next action: реализовать снимок и целевой регрессионный тест.

## LOG

### 2026-08-14 — Specification

Проверен актуальный `pilot/pilot.py`: `stage_attempts()` считает совпадения
стадии через `same_task_work`, а решения о лимите вызываются из одного прохода
`cycle()`. Следующая реализация должна сделать границу переданного списка
явной и доказать её тестом: добавленная после снимка задача видна только
следующему проходу. Product code и UI на этапе Specification не изменялись.
