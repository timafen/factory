# Provisioning Codex сохраняет закрытый доступ к авторизации

## Цель и влияние на пользователя

Авторизация Codex у всех воркеров должна оставаться доступной процессу пользователя
`factory`, но недоступной другим пользователям машины. Проверенные 11 рабочих путей
сейчас являются симлинками `root:root` с отображаемым режимом `777`; в Linux этот
режим не управляет доступом к цели. Все они указывают на
`/opt/factory-data/.codex/auth.json`, чей режим `600` и владелец `factory:factory`
защищают токен. Изменение устраняет неверный способ provisioning, не меняя рабочие
симлинки и не нарушая авторизацию действующих воркеров.

## Технический подход

Минимальная поставка находится во внешнем, не входящем в этот репозиторий,
provisioning-слое, который создаёт домашние каталоги воркеров и задаёт `CODEX_HOME`.
Он должен:

- создавать или обновлять симлинки от имени пользователя `factory`, не заменяя их
  отдельными копиями `auth.json`;
- до запуска воркера проверить метаданные конечной цели
  `/opt/factory-data/.codex/auth.json`: владелец и группа `factory:factory`, режим
  ровно `600`, обычный файл;
- при несоответствии завершаться ошибкой до запуска Codex и не выводить содержимое
  авторизационного файла в журнал;
- сохранить текущее направление симлинков на общий защищённый файл.

В Factory отсутствуют создание симлинков, настройка `CODEX_HOME` и внешний
provisioner; Go, Python, web-клиент, API и схема данных не меняются.

## План

1. Найти внешний provisioning, который создаёт окружение и домашние каталоги
   воркеров Codex.
2. Выполнить создание/обновление симлинков в контексте `factory`.
3. Добавить fail-closed проверку типа, владельца, группы и режима конечного
   `auth.json` до старта воркера; в диагностике показывать только путь и
   метаданные.
4. Проверить на затронутом воркере, что все ссылки продолжают разрешаться в один
   целевой файл, а `factory` может читать его без раскрытия содержимого.
5. Приложить к CARD-0047 результат команд проверки и закрыть инфраструктурную
   поставку отдельно от Factory.

## Критерии приёмки

1. Provisioning создаёт или обновляет симлинки от пользователя `factory`; данные
   авторизации не копируются в каталоги воркеров.
2. Перед запуском Codex provisioning отклоняет отсутствующий, не-обычный,
   не принадлежащий `factory:factory` или имеющий режим, отличный от `600`,
   целевой `auth.json`.
3. При корректной цели воркер продолжает авторизоваться через существующий
   симлинк; проверка не читает и не печатает токен.
4. В diff этого репозитория нет изменений реализации: исправление находится во
   внешней инфраструктуре, а CARD-0047 содержит ссылку на её доказательство.

## План проверки

- Внешний provisioner получает изолированный тест: корректная цель проходит,
  а неверные владелец, группа, режим, тип и отсутствие цели завершают его
  ненулевым кодом до запуска Codex.
- На целевой машине после provisioning выполнить только проверку метаданных:
  `stat -Lc '%F %a %U %G' /opt/factory-data/.codex/auth.json | grep -Fx 'regular file 600 factory factory'`.
- Проверить, что команда возвращает код 0 и её вывод не содержит токен.
- В этом репозитории обязательна только проверка документации:
  `git diff --check`.

## Риски и решения

- Исходник provisioning не найден в Factory, поэтому его путь, система запуска и
  тестовый фреймворк нельзя угадать; это требует владельца инфраструктуры.
- Не следует менять симлинки на копии auth-файлов: это размножит секрет и создаст
  риск рассинхронизации токенов. Отклонена также правка режима симлинка как
  неэффективная на Linux.
- Владение симлинком не заменяет проверки конечного файла; безопасностью токена
  управляют разрешение пути и метаданные конечной цели.
- Scope creep: изменение Codex CLI, процесса воркера или хранения токенов в
  Factory не требуется.

## Карточка

`knowledge/cards/CARD-0047-codex-auth-provisioning-ownership.md`

ГОТОВО-КОГДА: файл отсутствует в этом репозитории — внешний provisioner
ГОТОВО-КОГДА: команда stat -Lc '%F %a %U %G' /opt/factory-data/.codex/auth.json | grep -Fx 'regular file 600 factory factory'
