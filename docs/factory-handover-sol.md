# Передача управления Фабрикой: от старшей модели (Клод-Фейбл) → Сол (gpt-5.6-sol)
Дата: 2026-08-14 ~16:30Z. Причина: владелец переводит надзор на Сола (лимит Фейбла).
Ты — новый старший надзиратель. Владелец Тимофей — НЕ программист. Времена ему — Вашингтон (UTC-4).

## Железные правила владельца (нарушать нельзя)
1. Интерфейс никогда не врёт. 2. Технические вопросы/решения НЕ доходят до владельца —
решай сам, ему только настоящие продуктовые развилки. 3. Всё воспроизводимо через
репозиторий и выкат — НИКОГДА не править прод руками (исключение: диагностика читающая).
4. Всё видимое владельцу — по-русски, по-человечески, без хэшей и жаргона. 5. Уведомлений
минимум и по делу; писать ему при поломке или результате, не при каждом чихе. 6. Деньги
беречь: круги дороги, тесты бесплатны. 7. Активный надзор: дозор каждые 1-2 часа.

## Архитектура (проверенные факты)
- Сервер root@212.28.186.194. Панель https://factory.timafen.com (локально 127.0.0.1:7337, API без авторизации).
- factory-server (Go), factory-pilot (мозг, /opt/factory-data/pilot/pilot.py, цикл ~30с),
  factory-intake (:7338), ~20 воркеров (codex-*/claude-* × уровни), factory-release-broker
  (посредник выпусков, socket /run/factory/project-release-broker.sock, root:factory-release).
- Конвейер работ: Triage→Specification→Implement+Test→Review→Verify → squash-merge в main
  → мозг создаёт «поколение» (state.json: delivery_state_v2) → посредник исполняет
  /usr/local/lib/fx-factory-release → «выкачено». Заголовки задач: [auto] [N/5 Stage] <база>.
- Правки Плана/конфига ТОЛЬКО так: sudo -u factory python3, sys.path.insert(0,'/opt/factory-data/pilot'),
  import pilot; pilot.add_idea(kind,title,repo='factory',why=...,origin=...,order=N) + pilot.plan_idea(id).
  Иначе root-владение ломает пилота. Вопросы агентов: /opt/factory-data/pilot/questions/*.json —
  отвечать: status='answered', answer='...', answered_by='...' (от factory!).
- Выпуски вручную — ТОЛЬКО systemd-run --unit=... --collect (НИКОГДА nohup — глушит SIGHUP
  и вешает сцену ворот). Рейс через настоящую колею: curl --unix-socket
  /run/factory/project-release-broker.sock -X POST -H 'Content-Type: application/json'
  --data '{"operation_id":"<id>","adapter":"fx-factory-release","commit_sha":"<sha main>"}'
  http://release-broker/v1/operations ; статус: GET .../operations/<id>.
- Бортовой журнал каждого рейса: /var/lib/factory/release-broker/voyage-<id>.log — при
  провале там ВСЯ картина (это я добавила 14.08, до этого рейсы падали немыми).

## Известные болезни и их механика (доказано экспериментами 13-14.08)
1. ГЛОТАНИЕ ЗАВЕРШЕНИЙ (главная): мозг смотрит окно /tasks?limit=100 (pilot.py ~7249);
   work_lifecycle_block не находит «корень» работы, вытесненный патрулями из окна →
   boundary пустой → «поколение до явного повторного запуска» → tid попадает в processed
   БЕЗ создания следующего этапа (и запись в processed происходит ДО создания потомка —
   рестарт в зазоре тоже глотает). Симптом: конвейер «жив», но advanced нет; работы молчат.
   ЛЕЧЕНИЕ живьём: найти свежие succeeded-задачи работ (через /api/v1/tasks постранично),
   systemctl stop factory-pilot → убрать их id из state.json processed (от factory!) →
   start. Карточка постоянной починки: p1a0000d22f9 (+красные тесты пилота: p19ffdf1f856).
2. Пауза/перезапуск карточки: снятие паузы НЕ будит работу — нужен pilot.plan_idea(id)
   (новое поколение). Карточка p19ffd41eaf9.
3. Посредник после обновления его бинаря должен быть перезапущен — теперь выпуск делает
   это сам (влито и выкачено 14.08). Если увидишь deleted-inode в voyage-log — restart
   factory-release-broker и повтор рейса.
4. Денежный сторож: work_day_cap=80 (поднял владелец), max_work_rounds=4, deep_diag_rounds=3.
   Работа, севшая в пауза-по-деньгам, будится только plan_idea.
5. Песочница посредника: NoNewPrivileges=false обязательно (NNP+Private*/Protect* отнимает
   у root право менять пользователя). Закреплено в repo ops/systemd/ и drop-in'ом.

## Текущее состояние (16:30Z 14.08)
- ПОЕЗД: колея доказана целиком рейсом claude-train-drill-2 (succeeded, 13:38Z). Мозг сам
  стартует при вливании (5 раз доказано). ОСТАЛОСЬ: увидеть ЖИВОЙ самоход целиком —
  следующее вливание должно дойти до completed БЕЗ рук. ГЛАВНЫЙ НАКАЗ ВЛАДЕЛЬЦА:
  не упустить этот момент, досмотреть рейс до конца, доложить с доказательствами
  (merges.jsonl → generations phase=completed → voyage-log → factory-current.json).
- Кандидаты на вливание: «Вопросы про права…» (заслон техвопросов), «Снятая с паузы
  просыпается», «Сделано недавно показывает влитое» (+ТРЕБОВАНИЕ владельца: патрули
  в списках сделанного НЕ показывать ВООБЩЕ), «Перезапуск мозга не глотает» (p1a0000d22f9).
- Отчёты Фабрики: рендер чинён (npm i playwright в /opt/factory/web, EXIT=0). Пересобрать
  вчерашний отчёт и проверить завтрашний (экран «Ежедневные отчёты»). Мой отдельный
  ежедневный PDF-триггер (13:00Z) работает на Клоде — при исчерпании Фейбла заглохнет,
  замени своим или отключи (trig_01XnfDcDy25zaGqQeokiGC1B).
- Грок: агент Grok Build установлен (root и /opt/factory-data/.grok, вход по подписке
  владельца, модели grok-4.5/4.6). Ступень-2 (теневой мозг на 4.5) — владелец ещё НЕ дал добро.
- Замер «10 работ без рук» — ждёт задач владельца по новому проекту.
- Карточка «учётная запись боевого выпуска» (p19ff1df0ec3-тема) — ждёт решения владельца.
- Тестовые артефакты: /var/tmp/fairtest (3 честных теста моделей), /var/tmp/race5
  (пятиборье), /var/tmp/brainexam. Итог: Сол-5.6 лучший по надёжности — потому ты и здесь.

## Мозговая цепочка и деньги
brain_chain: gpt-5.6-sol → gpt-5.6-terra → opus → fable (config.json). Недельная квота
кодекса была ~9%. Секреты (токены) — /etc/factory-access/, значения НИКОГДА не показывать.
GitHub push: клон /root/fix-gate-repo с токеном. Свои коммиты подписывай и веди только
через repo+выкат. Дозор: журнал пилота, merges, generations, вопросы, паузы — каждые 1-2ч.
