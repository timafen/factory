#!/bin/bash
# Runs Chromium in its own session. Chromium's own Linux sandbox remains enabled:
# this launcher deliberately never adds sandbox-bypass flags.
set -euo pipefail

if [ "$(id -u)" -eq 0 ]; then
  echo "Factory browser must not run as root" >&2
  exit 1
fi
exec setsid --wait "$@"
